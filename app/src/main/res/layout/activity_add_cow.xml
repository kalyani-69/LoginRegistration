<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:fillViewport="true">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- ImageView for cow image -->
        <ImageView
            android:id="@+id/cowImageView"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_gravity="center"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_launcher_background"
            android:scaleType="centerCrop"
            android:background="@android:color/darker_gray" />

        <!-- EditText for cow name -->
        <EditText
            android:id="@+id/cowName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Cow Name"
            android:inputType="text" />

        <!-- EditText for cow color -->
        <EditText
            android:id="@+id/cowColor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Color"
            android:inputType="text" />

        <!-- EditText for pregnancy date -->
        <EditText
            android:id="@+id/pregnancyDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Pregnancy Date (dd/MM/yyyy)"
            android:inputType="date" />

        <!-- EditText for medical history -->
        <EditText
            android:id="@+id/medicalHistory"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Medical History"
            android:inputType="textMultiLine" />

        <!-- EditText for milk production -->
        <EditText
            android:id="@+id/milkProduction"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Milk Production (L/day)"
            android:inputType="numberDecimal" />

        <!-- Button to save cow details -->
        <Button
            android:id="@+id/saveCowButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Save Cow" />
    </LinearLayout>
</ScrollView>
